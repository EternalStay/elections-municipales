{% extends 'base.html.twig' %}

{% block title %}Modifier une Parti{% endblock %}

{% block body %}
    <h1>Modifier une Parti</h1>
    <a class="badge badge-dark mb-4" href="{{ path('parti_index') }}">> Retourner à la liste <</a>

    {{ include('parti/_form.html.twig', {'button_label': 'Modifier'}) }}
    
    {% if is_granted('ROLE_ADMIN') %}
    	{{ include('parti/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
